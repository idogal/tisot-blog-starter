backend:
  # Use netlify identity as backend
  # name: git-gateway
  branch: main
  ### enable below lines for github integration ###
  name: gitlab
  repo: ido.g/IdoG-Blog-DaisyUI
  # site_domain: cms.netlify.com
  # api_root: https://gitlab.com/ido.g/portfolio-design/api/v4
  # base_url: https://gitlab.com/ido.g/portfolio-design/
  # auth_endpoint: oauth/authorize
  # cms_label_prefix:
  auth_type: pkce
  app_id: ec6a9ae381b7e2a374302647071d9e7ed2768a08641b26b5e5dfabab83e9563a

media_folder: "src/assets/img/"
public_folder: "/assets/img"
publish_mode: editorial_workflow
site_url: https://ido.g.gitlab.io/idog-blog-daisyui/
display_url: https://ido.g.gitlab.io/idog-blog-daisyui/
logo_url: https://ido.g.gitlab.io/idog-blog-daisyui/assets/favicon/android-chrome-512x512.png

# Please run "npx netlify-cms-proxy-server" for local backend
# local_backend: true
# publish_mode: editorial_workflow

collections:
  - label: "Post"
    name: "post"
    folder: "/src/posts"
    extension: "md"
    format: "frontmatter"
    media_folder: "/src/assets/img/posts/"
    public_folder: "/assets/img/posts/"
    create: true
    slug: "{{slug}}"
    preview_path: "/posts/{{slug}}"        
    editor:
      preview: true
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Published by", name: "published_by", widget: "string" }
      - { label: "Publish Date", name: "publish_date", widget: "datetime" }
      - { label: "Trip Date", name: "trip_date", widget: "datetime" }
      - { label: "Trip Type", name: "trip_type", widget: "string" }
      - { label: "Main Image", name: "head_image", widget: "image" }
      - { label: "Thumbnail Image", name: "thumbnail", widget: "image" }
      - { label: "Tags", name: "tags", widget: "list", default: [], allow_add: true, field: {label: Tag, name: tag, widget: string} }
      - { label: "Body", name: "body", widget: "markdown" }
  - label: "Home Page"
    name: "home_page"
    folder: "/src/home_page"
    create: false
    # slug: "{{slug}}"
    # preview_path: "{{slug}}"
    fields:
      - { label: "Main Heading", name: "main_heading", widget: "string" }
      - { label: "Secondary Heading", name: "secondary_heading", widget: "string" }